@{
    ViewData["Title"] = "Listado Categorías";
}

<h1>Listado de Cargos</h1>

<div>
    <table class="table" id="table-workStation">
        <thead>
            <tr>
                <th data-formatter="rowNumFormatter">#</th>
                <th data-field="name">Nombre del Cargo</th>
                <th data-field="levelId">Nivel</th>
            </tr>
        </thead>
        <tbody>

        </tbody>
    </table>

</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        const UrlBase = '@Url.Action("GetList")';
        let $tableMain = $('#table-workStation');
    </script>

    <script src="~/js/WorkStation/index.js"></script>
}